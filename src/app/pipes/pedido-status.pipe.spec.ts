import { PedidoStatusPipe } from './pedido-status.pipe';

describe('PedidoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new PedidoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
